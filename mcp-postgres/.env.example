# PostgreSQL Connection String (REQUIRED)
# Format: postgresql://[user[:password]@][host][:port][/dbname][?param1=value1&...]
POSTGRES_URL=postgresql://user:password@localhost:5432/database

# Access Mode: readonly | readwrite (default: readonly)
# Use readonly mode in production for safety
ACCESS_MODE=readonly

# Logging Level: DEBUG | INFO | WARN | ERROR | NONE (default: INFO)
LOG_LEVEL=INFO

# Optional: Connection pool configuration via URL parameters
# Example with custom pool settings:
# POSTGRES_URL=postgresql://user:password@localhost:5432/database?max=10&idle_timeout=60000
#
# Available parameters:
# - max: Maximum number of connections (default: 5)
# - min: Minimum number of connections (default: 1)
# - idle_timeout: Idle timeout in milliseconds (default: 30000)
# - connection_timeout: Connection timeout in milliseconds (default: 10000)
