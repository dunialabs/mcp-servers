GATEWAY_CONFIG='{
  "apis": [{
    "name": "jsonplaceholder",
    "description": "Public placeholder API for quick testing",
    "baseUrl": "https://jsonplaceholder.typicode.com",
    "auth": { "type": "none" },
    "tools": [{
      "name": "getPost",
      "description": "Get a sample post by ID",
      "endpoint": "/posts/{id}",
      "method": "GET",
      "parameters": [{
        "name": "id",
        "description": "Post ID",
        "type": "number",
        "required": true,
        "location": "path"
      }],
      "response": { "type": "json" }
    }]
  }]
}'
LOG_LEVEL=INFO
