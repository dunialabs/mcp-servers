# Google Docs MCP Server Configuration

# Server Configuration
SERVER_NAME=mcp-google-docs
SERVER_VERSION=1.0.0

# Node Environment
NODE_ENV=development

# Logging (debug, info, warn, error, none)
LOG_LEVEL=info

# Google OAuth Access Token
# This is typically provided by PETA Core at runtime
# accessToken=ya29.xxx

# MCP Transport Mode (stdio or http)
MCP_TRANSPORT=stdio
